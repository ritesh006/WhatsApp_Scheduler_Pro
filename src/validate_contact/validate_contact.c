#include <stdio.h>
#include <string.h>
#include "image_reader.h"

int validate_contact(const char* number, const char* country) {
    // Your code here to:
    // 1. Check if number is valid (length, digits only)
    // 2. Check if country code is valid
    // 3. Return 1 if valid, 0 if not

printf(MAGENTA"IN VALIDATE CONTACT"RESET);

    return 0;
}

int read_config(const char* config_file, WhatsappContact* contact) {
    // Your code to:
    // 1. Open and read config file
    // 2. Parse contact details
    // 3. Validate them
    return 0;
}